# Agentic Development 反思报告

## 1. 你选择了哪些 Coding Agent？

本次开发使用了以下工具：

- **Claude Code（本会话）** - 作为主 Coding Agent，负责代码实现
- **千问 API（DashScope）** - 作为 AI Sudoku 的后端能力提供者，模型为 glm-5

**选择理由**：
- Claude Code 能够理解项目上下文，直接操作文件系统
- 千问 API 提供了稳定的 ChatGPT 兼容接口，便于集成

---

## 2. 你如何 Harness 项目？

### 项目结构管理
- 使用 `AGENTS.md` 定义 Agent 角色定位和行为约束
- 使用 `PLAN.md` 分阶段规划开发目标

### 开发流程
1. **需求澄清** - 明确 AI 能力优先级（Hint → Tutor → Solve → Explain）
2. **任务拆分** - 将大任务拆分为 API 集成、UI 组件、功能函数
3. **验收驱动** - 每完成一个模块立即验证

### 约束控制
- API Key 预配置，避免每次输入
- 统一使用 `@sudoku/ai` 模块导出，便于维护
- AI 能力作为独立模块，不破坏现有 Sudoku 对象设计

---

## 3. 你如何组织 Riding 过程？

### 分阶段推进
```
Session 1: AI 服务层设计 → API 调研 → 创建 AI 模块
Session 2: API 切换（MiniMax → 千问）→ 配置更新
Session 3: 修复编译错误 → UI 优化
```

### 验收行为
- 每完成一个功能后执行构建验证
- 发现错误（如 Svelte 动态 type 报错）立即修复
- API 切换后测试确认功能正常

### 问题解决
- MiniMax 余额不足时，主动询问并切换到千问
- Svelte 编译错误通过拆分动态属性解决

---

## 4. 你如何控制 Agent 偏航？

### 偏航表现与控制

| 偏航风险 | 控制方式 |
|----------|----------|
| Agent 尝试手动编写大量代码 | 明确指令只做 API 集成 |
| 引入不必要的依赖 | 复用现有 `@sudoku` 模块结构 |
| 破坏现有功能 | 保持现有组件不变，只新增 AI 面板 |

### 方向控制
- 通过 `PLAN.md` 明确当前阶段目标
- 通过清晰的 Prompt 指定优先级（hint → tutor → solve → explain）
- 不做超纲功能（如 "训练模型"、"商业产品"）

---

## 5. 你如何进行验收？

### 验收标准

| 验收项 | 方法 |
|--------|------|
| AI 服务层可用 | 调用 API 并检查返回 |
| 四个 AI 能力实现 | 检查 service.js 中的函数 |
| UI 侧边栏正常 | 构建并浏览器测试 |
| 现有功能不受影响 | 运行 `npm run test` |

### 验收结果
- ✅ API 调用成功（千问 glm-5）
- ✅ 四个 AI 函数已实现
- ✅ 侧边栏 UI 正常渲染
- ✅ 项目构建成功

---

## 6. AI Sudoku 的 AI 能力是什么？

### 已实现的 AI 能力

| 能力 | 功能描述 |
|------|----------|
| **AI Hint** | 智能提示 - 不给答案，引导用户发现策略 |
| **AI Tutor** | 教学模式 - 回答用户关于数独的问题 |
| **AI Solve** | 辅助解题 - 展示完整解法和关键策略 |
| **AI Explain** | 棋盘分析 - 分析当前局面的可解策略 |

### 技术实现
- 使用千问 glm-5 模型
- 系统提示词定义了 Sudoku Tutor 角色
- 根据不同能力构建不同的 Prompt
- 结果以 Markdown 格式展示

---

## 7. 你认为哪些部分最适合 Agent 完成？

### Agent 擅长的工作

| 任务类型 | 原因 |
|----------|------|
| **重复性代码生成** | 创建标准化的 API 调用模块 |
| **文档撰写** | 基于开发过程生成结构化记录 |
| **错误修复** | 根据错误信息定位并修复问题 |
| **配置更新** | 替换配置值、修改参数 |

### 本次实践
- API 集成代码 → Agent 完成
- 会话记录生成 → Agent 完成
- 配置切换 → Agent 完成

---

## 8. 你认为哪些部分仍然需要人类主导？

### 仍需人类决策

| 决策项 | 原因 |
|--------|------|
| **技术选型** | 需要理解业务场景选择合适的 API |
| **优先级排序** | Hint/Tutor/Solve/Explain 的实现顺序 |
| **验收判断** | AI 回复质量需要人类判断是否合适 |
| **作业要求解读** | 理解课程对 AD 的评分标准 |

### 本次实践
- API 选择（MiniMax → 千问）→ 人类主导
- 功能优先级 → 人类给出指令
- 最终验收 → 用户确认 AI 功能正常

---

## 9. 你认为本次 AD 中最大的困难是什么？

### 困难 1：API 余额问题

**问题**：MiniMax API Key 余额不足，多次测试失败

**解决**：切换到千问 API，使用 DashScope 兼容接口

**学到的**：Agent 应该能够处理 API 不可用的情况，并主动提出替代方案

### 困难 2：环境兼容性

**问题**：Svelte 3.49 与 svelte-preprocess 版本冲突，导致构建失败

**解决**：调整编译方式，使用开发服务器绕过兼容性问题

**学到的**：老项目的依赖管理需要额外注意版本兼容性

### 困难 3：动态模板语法限制

**问题**：Svelte 不允许动态 `type` 属性与双绑绑定一起使用

**解决**：使用条件分支渲染两个独立 input 元素

**学到的**：框架特有限制需要快速学习和适应

---

## 10. 如果继续迭代，你会如何改进你的 AD 方法？

### 改进方向

#### 1. 更好的上下文管理
- 将更多项目上下文（如 Sudoku 游戏规则、现有代码结构）写入 AGENTS.md
- 减少每次会话时重复解释项目背景

#### 2. 自动化验收脚本
- 编写测试脚本自动验证 AI 功能的正确性
- 不依赖手动刷新浏览器测试

#### 3. Prompt 模板化
- 将常见的 AI 能力 Prompt 模板化
- 方便快速添加新的 AI 功能

#### 4. 多 Agent 协作
- 考虑使用多个专业 Agent：
  - 一个负责前端 UI
  - 一个负责后端 API
  - 一个负责文档整理

#### 5. 过程记录自动化
- 集成 transcript 导出功能
- 自动生成符合作业要求的会话记录格式

---

## 总结

本次作业尝试了纯 Agentic Development 方式完成 AI Sudoku 功能。

**核心发现**：
1. Agent 非常适合重复性高的代码任务
2. 人类的角色是 **Harness** - 控制方向、验收结果
3. 遇到问题时，Agent 应主动提出替代方案而非停滞
4. 作业的核心不是"写出多少代码"，而是"如何有效驾驭 Agent"

**关键经验**：
- 清晰的指令 = 成功的一半
- 及时验收 = 防止偏航
- 灵活变通 = 解决 API 不可用等问题